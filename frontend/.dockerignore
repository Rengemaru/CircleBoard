# イメージに入れないもの。ビルドの入力を小さくし、秘密情報を持ち込まない。

# 依存はイメージの中で npm ci で入れ直す。ホスト側のもの(OSが違う可能性がある)は使わない
node_modules

# ビルド結果はイメージの中で作る
dist

# 秘密情報(CLAUDE.md §3-6)
.env
.env.*
!.env.example

.git
.gitignore

# 開発用のイメージ定義と起動スクリプト。本番イメージには要らない
Dockerfile*
.dockerignore
docker-entrypoint.dev.sh
